export const YOUTUBE_PLAYER_ID = 'youtube-player';

export const YOUTUBE_API_SCRIPT_ID = 'youtube-api-script';

export const INIT_PLAYER_INTERVAL_TIME = 500;

export const MAX_INIT_PLAYER_POLLING_TIME = 10000;

export enum YoutubePlayerStatus {
  WAITING = 'waiting',
  READY = 'ready',
  ERROR = 'error',
}
